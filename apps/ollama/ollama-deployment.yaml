apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      volumes:
        - name: models-storage
          persistentVolumeClaim:
            claimName: ollama-models-pvc
      containers:
        - name: ollama
          image: ollama/ollama:latest
          command: ["/bin/sh", "-c", "ollama serve & sleep 5 && ollama pull mistral && tail -f /dev/null"]
          ports:
            - containerPort: 11434
          volumeMounts:
            - mountPath: /root/.ollama/models
              name: models-storage
